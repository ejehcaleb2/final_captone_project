# Example environment configuration
# Copy this file to .env and update with your actual values

# Database Configuration
# Replace with your actual PostgreSQL credentials
DATABASE_URL=postgresql://username:password@localhost:5432/course_enrollment_db

# For Render deployment (free tier):
# 1. Create a free PostgreSQL instance on Render
# 2. Get the connection string and add it below
# DATABASE_URL=postgresql://your_user:your_password@your_host:5432/your_db_name

# JWT and Security
# Change this to a random secret key in production
SECRET_KEY=change-me-to-a-random-secret-key-in-production

# Token configuration
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Environment
ENVIRONMENT=development
